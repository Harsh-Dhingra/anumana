# Experiments

Hydra-configured experiment entry points live in `scripts/run_experiment.py`.
Run with config overrides, e.g.:

```
python scripts/run_experiment.py scenario=swarm_baseline tracker=jpda
```
